# GorkyGuide 🧭

[![Python](https://img.shields.io/badge/Python-3.10+-blue?style=for-the-badge&logo=python)](https://www.python.org/)
[![Aiogram](https://img.shields.io/badge/aiogram-3-green?style=for-the-badge&logo=telegram)](https://aiogram.dev/)
[![YandexGPT](https://img.shields.io/badge/YandexGPT-FFDB42?style=for-the-badge&logo=yandex&logoColor=black)](https://cloud.yandex.ru/services/yandexgpt)

Персональный AI-гид по Нижнему Новгороду в Telegram, использующий гибридный подход: надежность локальной базы данных и креативность YandexGPT.

## 🎥 Демонстрация Работы

![Демонстрация работы GorkyGuide](https://github.com/user-attachments/assets/4915b8b9-37c2-4c01-8f9c-607b5073691a)

## 🎯 О проекте

**GorkyGuide** — это прототип умного помощника для туристов, реализованный в виде Telegram-бота. Проект демонстрирует современную гибридную архитектуру, где локальная, верифицированная база данных культурных объектов сочетается с творческими возможностями большой языковой модели (LLM) для создания уникальных и, что самое главное, **достоверных** маршрутов.

Бот ведет пользователя по простому диалогу, определяя его интересы, доступное время и местоположение, чтобы на выходе предоставить готовый, красиво отформатированный план прогулки.

## ✨ Ключевые возможности

-   **🧠 Двухэтапная AI-логика:** Вместо одного большого запроса используется двухшаговая модель:
    1.  **AI-Классификатор:** Мгновенно определяет категорию интересов пользователя.
    2.  **AI-Рассказчик:** Творчески "упаковывает" уже отобранные из базы данных места в увлекательный маршрут.
-   **✅ Надежность данных:** Использование локального `data.json` файла полностью **исключает "галлюцинации" AI** — бот никогда не придумает несуществующее место или неверный адрес.
-   **🗣️ Управляемый диалог:** Бот ведет последовательный диалог с помощью машины состояний (Aiogram FSM), что делает взаимодействие интуитивно понятным.
-   **⚡ Полная асинхронность:** Все сетевые запросы выполняются неблокирующим способом через `aiohttp`, гарантируя отзывчивость бота.
-   **📄 Строгая работа с данными:** Взаимодействие с AI-рассказчиком построено на строгом `JSON`-формате, обеспечивая стабильность и предсказуемость результата.
-   **🎨 Динамическое форматирование:** Бот самостоятельно преобразует структурированные `JSON`-данные в красивое, читаемое сообщение с использованием `Markdown`.
-   ** удобный интерфейс:** Постоянная кнопка "Построить свой маршрут" делает повторное использование бота максимально простым.

## 🛠️ Технологический стек

-   **Язык:** `Python 3.10+`
-   **Telegram-бот:** `Aiogram 3`
-   **AI-модель:** `YandexGPT API`
-   **HTTP-клиент:** `aiohttp`
-   **База данных:** Локальный `JSON`-файл
-   **Конфигурация:** `python-dotenv`
-   **Логирование:** `logging` (с ротацией файлов)

## ⚙️ Как это работает?

Архитектура проекта — это конвейер по обработке данных:

1.  **Запрос интересов:** Пользователь нажимает кнопку "Построить маршрут" и описывает свои интересы.
2.  **AI-Классификатор:** Текст с интересами отправляется в YandexGPT с первым, простым промптом. Задача AI — вернуть **только цифру** наиболее подходящей категории (например, "4" для "Музеи").
3.  **Извлечение данных:** Бот использует полученную цифру как ключ для доступа к локальному файлу `data.json` и извлекает список всех объектов из этой категории.
4.  **Выборка:** Из полученного списка случайным образом выбираются 3-5 объектов для будущего маршрута.
5.  **Сбор оставшихся данных:** Бот запрашивает у пользователя время на прогулку и местоположение.
6.  **AI-Рассказчик:** **Отобранные** объекты и данные от пользователя упаковываются во второй, сложный промпт. Задача AI — не придумывать места, а **сплести из готовых данных** увлекательный рассказ, таймлайн и вернуть все это в строгом формате `JSON`.
7.  **Обработка и форматирование:** Бот получает `JSON`, очищает его от возможных артефактов и с помощью специальной функции-форматтера превращает в идеально сверстанное сообщение для Telegram.
8.  **Отправка результата:** Готовый маршрут отправляется пользователю.

## 🚀 Локальный запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/GorkyGuide.git
    cd GorkyGuide
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # Для Windows: venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Подготовьте данные:**
    Убедитесь, что в корневой папке проекта находится файл `data.json` с необходимой структурой.

5.  **Настройте переменные окружения:**
    Создайте файл `.env` в корневой папке по образцу `.env.example` и добавьте в него свои ключи:
    ```env
    BOT_TOKEN="ВАШ_ТЕЛЕГРАМ_БОТ_ТОКЕН"
    YANDEX_GPT_API_KEY="ВАШ_API-КЛЮЧ_ОТ_YANDEX_CLOUD"
    FOLDER_ID="ВАШ_ИДЕНТИФИКАТОР_КАТАЛОГА_В_YANDEX_CLOUD"
    ```

6.  **Запустите бота:**
    ```bash
    python bot.py
    ```

## 🔮 Возможные улучшения

-   **Интерактивные карты:** Добавление к каждой точке маршрута кнопки "Показать на карте" с использованием координат из `data.json`.
-   **Фильтрация по геолокации:** Вместо случайного выбора объектов — выбирать те, что находятся ближе всего к стартовой точке пользователя.
-   **Обратная связь:** Возможность для пользователя оценить предложенный маршрут (👍/👎) для сбора статистики.
-   **Расширение базы данных:** Легкое добавление новых категорий и объектов в `data.json` без изменения кода.